- v1.14
  - Major update!
  - Adds multiplayer support. All settings now only affect your client.
  - Adds automatic calculation to setting "Real terrain visibility" (when set to 0).
  - Changes the name of zone based settings and changes the numbering system (from 1..n to 0..n).
  - Fixed for the new update.
  - Fixes spawners not working on forced active zones (not tested).
  - Removes the setting "Spawn limit" as confusing. It's now automatically determined.

- v1.13
  - Fixes Force active not working on single player (multiplayer of course doesn't work).

- v1.12
  - Fixed for the new game version.

- v1.11
  - Fixes some settings being reset on game restart.

- v1.10
  - Fixed for the new game version.
